# Optional. The official public endpoint is used when this is empty.
# For a deployed demo, use a private Base RPC URL from a node provider.
BASE_RPC_URL=https://mainnet.base.org

# Added in milestone two. Never expose this in browser code or commit it.
ETHERSCAN_API_KEY=

# Added in milestone two. The core evidence engine works without a model.
AI_API_KEY=
AI_MODEL=
